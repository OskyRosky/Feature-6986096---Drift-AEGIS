# AEGIS Forecast Drift — Engineering Roadmap (E0-E8)

**Feature 6986096.** Adopted 2026-07-12 (Oscar). This engineering roadmap is used to **build** the product. The Blueprint remains the conceptual source of truth.

> **Naming discipline:** `E`-stages = engineering phases (below). `V1/V2/V3` = product/document versions used by the README and Blueprint. Keep them separate to avoid the pre-existing "V1" collision (Blueprint V1, README V1=Power BI, folders V1/V2/V3).

| Stage | Name | Nature | Status |
| --- | --- | --- | --- |
| **E0** | Foundation — Blueprint, Mockup, Git, Governance | Docs/setup | ✅ Complete |
| **E1** | Source Discovery & Data Profiling | Discover (no calc) | ◑ In progress |
| ├ E1A | Document Discovery & Reuse (no live SQL) | Docs from existing knowledge | ✅ Complete |
| └ E1B | Live Data Validation (read-only SQL) | Confirm hypotheses | ✅ Complete (all 4 families computable) |
| **E2** | Forecast Drift Information Model (tables, grain, relations, lineage) | Design | ✅ Complete |
| **E3** | Mathematical Drift Model (formulas + known-answer fixtures) | Math | ✅ Complete |
| **E4** | Output Schema Design — `aegis_forecast_drift_signals` (design only) | Design | ✅ Complete |
| **E5** | SQL Implementation & Validation (views/CTEs/procs + validate vs fixtures) | Build | ✅ Complete |
| ├ E5A | Python Drift Engine (read-only ingest + calc + governed datasets) | Build | ✅ Complete |
| └ E5B | **Production Dataset Validation & Export Hardening** (canonicalization, atomic export, refresh runner, contract) | Build | ✅ Complete — offline hardening + expanded live read-only run validated (2026-07-13) |
| **E6** | Power BI MVP (consume only) | Viz | ◑ In progress — model `AEGIS_Forecast_Drift` refreshed + validated in Desktop (in-model 168/672/71/1, 24 measures=Python, 4 rels); TMDL at `V1/PBI/tmdl/`; save `.pbix` + author visuals pending (PARTIAL) |
| **E7** | Grafana Production Dashboard (same SQL, new viz) | Production | ◑ In progress |
| ├ E7A | Grafana Readiness & Data Source (Infinity + read-only CSV server) | Build | ✅ Complete (2026-07-16) — existing Grafana 13.0.1 preserved; Infinity 3.10.1 + datasource `AEGIS Forecast Drift CSV`; counts 168/672/71/1 served internally |
| ├ E7A.1 | Infinity Functional Query Validation Gate (in-Grafana query per dataset) | Validate | ✅ Complete (2026-07-17) — manual authenticated Grafana queries; Query Inspector 168/672/71/1; health check OK; CSV/null parsing PASS; type hints → E7C/E7D. Token `E7A_INFINITY_QUERY_GATE_COMPLETED` |
| ├ E7A.2 | V2 Governed Data Snapshot & Datasource Rewire (self-contained V2) | Build | ✅ Complete (2026-07-17) — byte-equivalent SHA256-verified snapshot under `V2/data/processed/` + `sync-governed-data.ps1`; `aegis-csv` rewired to V2 (read-only); V1 untouched; Grafana/UID/port unchanged |
| ├ E7B.0 | Formal Closure of E7A.1 (docs + evidence, no MCP) | Docs | ✅ Complete (2026-07-17) — evidence registered; V14 DEFERRED→PASS; closure docs. Token `E7B0_E7A1_FORMAL_CLOSURE_COMPLETED` |
| ├ E7B.1 | MCP Connection Preflight (assess prerequisites only) | Build | ✅ Complete (2026-07-17) — documentation-only (no runtime mutation); nothing installed/configured. `mcp-grafana` v0.17.2 (Grafana ≥9.0 ✓); **Arch A** host binary/stdio/`localhost:3000`/Claude local scope/token-file; `aegis-mcp` least-privilege design; tool allow-list; E7B.2–E7B.5 plan. No native Infinity query tool noted. Token `E7B1_MCP_PREFLIGHT_COMPLETED`. No blockers for E7B.2 |
| ├ E7B.2 | Install pinned MCP server (v0.17.2, no secrets) | Build | ✅ Complete (2026-07-17) — official `mcp-grafana` v0.17.2 (windows/amd64) installed **outside repo** at `%LOCALAPPDATA%\AEGIS\mcp-grafana\v0.17.2\`; SHA256 `939eb0f4…eb62616` verified vs release page + `checksums.txt`; `--version`/`--help` OK (stdio + `--disable-write`); secret-free scripts in `V2/scripts/`. No Grafana connection, no token, no MCP registration. Token `E7B2_MCP_INSTALLATION_COMPLETED`. No blockers for E7B.3 |
| ├ E7B.3 | Service account `aegis-mcp` + short-lived token (manual) | Build | ✅ Complete (2026-07-18) — service account `aegis-mcp` (login `sa-1-aegis-mcp`, role **Editor**, not admin) + short-lived token (explicit expiration). `GRAFANA_SERVICE_ACCOUNT_TOKEN_FILE` verified supported in v0.17.2 but rejected (plaintext); token stored **DPAPI-encrypted outside repo** (`%LOCALAPPDATA%\AEGIS\secrets\grafana\`), decrypted in-memory only. Four secret-free scripts in `V2/scripts/`. Authenticated **read-only** validation PASS (datasource `aegis-forecast-drift-csv` visible; no writes; no MCP registration). Token `E7B3_SERVICE_ACCOUNT_TOKEN_COMPLETED`. No blockers for E7B.4 |
| ├ E7B.4 | Register MCP in Claude Code + connectivity smoke test | Build | ✅ Complete (2026-07-18) — `mcp-grafana` v0.17.2 registered in Claude Code as stdio server `grafana`, **local scope** (outside repo), command = secure wrapper (`Environment: empty`, token from DPAPI in memory). Wrapper hardened for clean stdio. Standalone smoke test (`initialize`+`list_datasources`) returned `aegis-forecast-drift-csv`; `claude mcp list` = **✓ Connected**. No dashboards/writes; E7C not started. Token expires 2026-09-01, revoke at E7D close. Token `E7B4_MCP_REGISTRATION_SMOKE_COMPLETED`. No blockers for E7B.5 |
| └ E7B.5 | MCP connection closure (operational readiness, non-destructive) | Build | ✅ Complete (2026-07-18) — **NON-DESTRUCTIVE** closure (scope corrected): token & MCP kept active for E7C/E7D. Did **not** revoke token / delete DPAPI / run remove script / `claude mcp remove` / unregister. Re-verified: MCP `grafana` **✓ Connected** (Environment empty); datasource `aegis-forecast-drift-csv` visible; no secrets in repo/config/git; DPAPI present outside repo; token expires 2026-09-01, **definitive revocation at E7D close**; Grafana running; no dashboards; `HEAD==origin==4b7c6cc`. Token `E7B5_MCP_OPERATIONAL_READINESS_COMPLETED`. No blockers for E7C |
| ├ E7C | Grafana Dashboard **Foundation Preview** (folder + preview dashboard via MCP) | Build | ✅ Complete (2026-07-18) — folder `AEGIS Forecast Drift` (uid `afsjccp27s0e8d`) + dashboard `AEGIS Forecast Drift` (uid `aegis-forecast-drift-foundation`) created via MCP. Header + 4 Infinity query variables (forecast_key 12 / region 9 / drift_status 4 / run_id 1, all with All) + 4 preview panels (Latest Run, Total Signals=168, Status Distribution H82/W38/Wa34/C14, Top 10 by drift score). Datasource `aegis-forecast-drift-csv` health OK; read-only `/api/ds/query` per panel rows 1/168/168/168 (no No-data); pre-existing `advs2xz` untouched; governed JSON export `V2/grafana/dashboards/aegis-forecast-drift-foundation.json` (secret-free, UID ref). No E7D, no alerts, no threshold/weight/data change; token not revoked. Token `E7C_DASHBOARD_FOUNDATION_PREVIEW_COMPLETED`. URL `http://localhost:3000/d/aegis-forecast-drift-foundation/aegis-forecast-drift`. Awaiting visual review before E7D |
| ├ E7D.0 | Dashboard Information Architecture & Shared Navigation (structure only) | Build | ✅ Complete (2026-07-19) — **product backbone**: 11 dashboards in folder `AEGIS Forecast Drift` (uid `afsjccp27s0e8d`) sharing one navigation contract, one shared-filter contract and one visual design system. Overview = E7C dashboard retitled `AEGIS Forecast Drift — Overview` (uid `aegis-forecast-drift-foundation` **retained**; proposed `-overview` not adopted to avoid deleting the existing dashboard) with shared nav, corrected `Forecast Key` label + new `forecast_version` var. 10 **shells** created (Forecast, Performance, Shape, Stability, Volatility, Events, Historical Timeline, **Top Forecast Keys** [replaces mockup *Top Services* — `service` empty], **Top Scenarios** [single `Enterprise`], Settings & Data Quality) — nav + applicable shared filters + purpose panel, **no analytical panels**. Shared filters data-driven (region 9/forecast_key 12/forecast_version 14/drift_status 4/run_id 1, no empties). Published via `V2/scripts/push-e7d0-structure.ps1` (token DPAPI in-memory): Overview v2, shells v1, all `success`/`inFolder`; foreign `advs2xz` untouched; datasource unchanged; MCP `✓ Connected`; repo secret-literal scan CLEAN. No E7D.1, no alerts, no threshold/weight/data change. Token `E7D0_INFORMATION_ARCHITECTURE_COMPLETED`. Deliverables `engineering/E7_grafana/E7D0_*` (8 docs) + 11 governed JSON. Awaiting visual review before E7D.1 |
| ├ E7D.1 | Overview MVP (analytical panels) | Build | ● **Completed — visually accepted** (2026-07-19) — transformed the Overview (uid `aegis-forecast-drift-foundation` **retained**) from foundation-preview into a full analytical dashboard: **7 components / 15 panels** — KPIs (Total Signals 168 · Total Events 71 · Avg Drift 28.8 · Critical 14 · Warning 34 · Watch 38 · Healthy 82), Drift Status donut (82/38/34/14), Avg Drift over time (14 forecast-version points, thresholds 20/40/70, UTC), Signals by dominant family (Stability 88 · Volatility 45 · Shape 27 · Performance 8, neutral color), Forecast Keys by drift risk (12 keys, avg desc NAM-SDF 42.74, max-status band), Latest Governed Run (1 · Success · 2026-07-13 22:44 UTC · 168 · 71), Data Quality 18/18. All 5 shared filters bound (FEXPR, backend parser); **fixed** Infinity date-arithmetic bug by typing `forecast_version` as `timestamp` + quoted `run_id`. **The first build (v3) rendered "No data" on every panel in live Grafana** — root cause: (1) the 5 template variables lacked the `queryType:"infinity"` wrapper (plugin treated them as legacy → empty dropdowns → `region IN ()` → backend error on all panels); (2) panels 23/24 aliased a filtered column (`No parameter 'forecast_key'/'calculation_run_id' found`). **Both fixed** (variable wrapper restored; filtered column `text`==selector, display via rename/`displayName`), republished **v5**. A **second defect** then surfaced — the `forecast_version` variable alone red-triangled because Grafana's frontend infers its **ISO-date** option values as `time`, emptying it → `forecast_version IN ()` → all signal panels "No data" again. **Fixed (v6)** by filtering on a non-date **`v`-prefixed label** (`fv_label = 'v' + forecast_version`) across the variable + 10 signal panels; the trend panel keeps `forecast_version:timestamp` for its X-axis and drops the version clause. Broken build archived at `V2/grafana/dashboards/archive/`, republished **v6**. Post-repair **end-to-end per-panel replay of published targets (All-expanded): 13/13 PASS, 0 FAIL**; analytical reconciliation 21/21. **Oscar visually confirmed** the data, then requested a final **visual polish (v7)** — donut severity colors (Healthy green / Watch yellow / Warning orange / Critical red), a 2024-anchored time axis (`2024-03-01 → 2026-06-01`, no empty 2022 gap), a slimmer nav/header (`h:3→h:2`, no content/links removed, panels reflowed), and a corrected filter-semantics note (KPIs/distribution/families/ranking honor all five filters; the **trend intentionally ignores Forecast Version** to preserve full history). Polish re-validated **13/13 PASS**, republished **v7**; **Oscar formally accepted**. `success`, `inFolder`; foreign `advs2xz` untouched; datasource/nginx/Docker/CSV/Python/PBI/weights/thresholds unchanged; no alerts/plugins; no dashboard deleted; **no manual commit**; other 10 dashboards untouched. Token `E7D1_OVERVIEW_MVP_COMPLETED_VISUALLY_ACCEPTED`. Deliverables `engineering/E7_grafana/E7D1_*` (5 docs) + updated Overview JSON + broken-build archive + publish script. URL `http://localhost:3000/d/aegis-forecast-drift-foundation`. Do not start E7D.2 until authorized |
| ├ E7D.2 | Forecast MVP (analytical panels) | Build | ✅ **Visually accepted** (2026-07-19) — rebuilt the Forecast dashboard (uid `aegis-forecast-drift-forecast` **retained**) from the E7D.0 shell into a full analytical dashboard: **8 components / 10 panels** — Overall Forecast Drift Score gauge (mean 28.8, bands 20/40/70), Drift Family Distribution donut (Stability 88 · Volatility 45 · Shape 27 · Performance 8, **neutral** colors), Drift Status Distribution donut (Healthy 82 · Watch 38 · Warning 34 · Critical 14, **severity** colors), Avg Drift Score over time (14 forecast-version points, thresholds 20/40/70, UTC — **ignores Forecast Version filter**), Forecast Keys by avg drift score (12 keys, desc, NAM-SDF 42.74 top), **Drift Score Heatmap** (12 keys × 14 versions, one governed score per cell, severity background — single-query pivot via the native `groupingToMatrix` transform, since Grafana has no native categorical pivot), Latest Governed Run (1 · Success · 2026-07-13 22:44 UTC · 168 · 71), Data Quality 18/18. **Reused the stable E7D.1 mechanism verbatim** (`queryType`-wrapped variables, `v`-prefixed `fv_label`, quoted `run_id`, no aliasing of filtered columns, transform aggregation). Reconciliation **18/18** (8 panel + 4 consistency + 6 filter: All→168, Critical→14, NAM-SDF→14, NAM→42, v2025-12-01→12 mean 53.3); pre-publish validator **0 failures**; heatmap gate **14/14**×12 rows (single batch). **Oscar's first visual review (v2)** flagged two defects — status donut colored by position (palette-classic) and the heatmap rendered empty (14-target Infinity pivot hit per-URL response coalescing). **Both fixed:** donut now uses explicit `byName` severity overrides; heatmap rebuilt as a **single-query `groupingToMatrix` pivot** (one query, no coalescing, honors shared filters). Published **only** Forecast via `V2/scripts/push-e7d2-forecast.ps1` (token DPAPI in-memory) → **v3**, `success`, in folder, 10 panels; shell archived. Overview + other 9 dashboards, datasource, nginx, Docker, CSVs, Python, Power BI, weights, thresholds, alerts, plugins, token, DPAPI, MCP **untouched**; repo secret-free; no manual commit. **Oscar's second visual review (2026-07-19) accepted both repairs** (status donut severity colors + heatmap 12×14 with values/colored backgrounds + all other panels correct). Token `E7D2_FORECAST_MVP_COMPLETED_VISUALLY_ACCEPTED`. Deliverables `engineering/E7_grafana/E7D2_forecast_*` (6 docs) + rebuilt JSON + shell backup + push script. Next: E7D.3 (not started) |
| ├ E7D.3 | Performance MVP (analytical panels) | Build | ✅ **Completed — visually accepted** (2026-07-19) — rebuilt the Performance dashboard (uid `aegis-forecast-drift-performance` **retained**) from the E7D.0 shell into a full analytical dashboard for the performance drift family (weight 20%): **9 components / 11 panels** — Average Performance Drift Score (**7.71**, bands 20/40/70), Performance Signals Computable (**156**), Average MAPE Change (**0.81 %**, `MAPE_deep`, percent), Performance Coverage (**92.9 %** = 156/168, green ≥ 0.9), Performance Drift Score over time (13 forecast-version points, thresholds 20/40/70, UTC — **ignores Forecast Version**; peaks 38.19 @ 2025-06-01, 23.19 @ 2026-03-07), Performance Signal Details (per-signal, score desc, Computable Yes/No, severity status), Non-computable Performance Summary (**NO_REALIZED_OVERLAP 12** from family scores), Latest Governed Run (1 · Success · 2026-07-13 22:44 UTC · 168 · 71), Data Quality 18/18. **No value hardcoded.** **Computability predicate solved** (score empty for non-computable rows; Infinity numeric `>= 0` → HTTP 400 / truthy-empty and ternary computed columns unsupported): select score as **string**, filter `performance_drift_score != ''` (156), `convertFieldType` → number for mean/trend, boolean computed flag for coverage. **Reused the E7D.1/E7D.2 mechanism verbatim** (`queryType`-wrapped variables, `v`-prefixed `fv_label`, quoted `run_id`, no aliasing of filtered columns, transform aggregation); Run + DQ cloned 1:1 from Forecast. Reconciliation **all matched, 0 mismatches** (4 KPI + 4 consistency + 13-bucket trend + details + 7 filter scenarios: All→156/7.71, NAM-MSIT→13/18.39, region NAM→39, v2025-06-01→12/38.19, Critical→14/30.03, missing key→0); pre-publish validator **0 failures**. Published **only** Performance via `V2/scripts/push-e7d3-performance.ps1` (token DPAPI in-memory) → **v2**, `success`, in folder, 11 panels; shell archived at `V2/grafana/dashboards/archive/aegis-forecast-drift-performance-shell.json`. Documented limitations: only `MAPE_deep` rows expose Current/Previous MAPE (details blank elsewhere by design); Non-computable Summary honors only Forecast Key + Forecast Version (family scores lack region/status/run_id). Overview + Forecast + other 8 dashboards, datasource, nginx, Docker, CSVs, Python, Power BI, weights, thresholds, alerts, plugins, token, DPAPI, MCP untouched; repo secret-free; no manual commit. Agent browser unauthenticated → **live render awaits Oscar**. **Oscar's v2 visual review found one defect — Performance Coverage rendered empty — now fixed in v3:** `is_comp` (`performance_drift_score != ''`) came back **boolean** so the `mean` reducer yielded no value; added a `convertFieldType is_comp → number` transform → **92.9 %**. Denominator confirmed dynamic (All 168, Key 14, Region NAM 42, Version 12), not hardcoded. Republished **only** Performance → **v3**, `success`, in folder, 11 panels. **Oscar visually accepted v3 on 2026-07-19** (Coverage 92.9 %, all other components correct). Token `E7D3_PERFORMANCE_MVP_COMPLETED_VISUALLY_ACCEPTED`. Deliverables `engineering/E7_grafana/E7D3_performance_*` (5 docs) + rebuilt JSON + shell archive + push script. URL `http://localhost:3000/d/aegis-forecast-drift-performance`. Do **not** start E7D.4 |
| ├ E7D.4 | Shape MVP (analytical panels) | Build | ✅ **Completed — visually accepted** (2026-07-19) — rebuilt the Shape dashboard (uid `aegis-forecast-drift-shape` **retained**) from the E7D.0 shell into a full analytical dashboard for the shape drift family (weight 40%): **8 components / 10 panels** — Average Shape Drift Score (**26.03**, bands 20/40/70), Shape Signals Computable (**168**), Shape Coverage (**100.0 %** = 168/168, green ≥ 0.9), Maximum Shape Drift Score (**100.00**), Shape Drift Score over time (14 forecast-version points, UTC — **ignores Forecast Version**; peak 62.60 @ 2025-12-01, secondary 44.08 @ 2026-03-07, low 9.59 @ 2026-04-16), Shape Signal Details (per-signal, score desc, full width), Latest Governed Run (1 · Success · 2026-07-13 22:44 UTC · 168 · 71), Data Quality 18/18. **No value hardcoded.** **Data-driven scope:** Shape is 168/168 computable (0 non-computable) → Non-computable Summary panel **intentionally omitted** (Details widened to full width); the snapshot exposes a single `shape_drift_score` with no auxiliary/MAPE metric → 4th KPI is **Maximum Shape Drift Score** and no Current/Previous columns. **Reused the E7D.1/E7D.2/E7D.3 mechanism verbatim** (`queryType`-wrapped variables, `v`-prefixed `fv_label`, quoted `run_id`, no aliasing of filtered columns, transform aggregation; E7D.3 boolean fix `convertFieldType is_comp → number` before `mean` applied to Coverage); Run + DQ cloned 1:1. Structural validator **0 failures**; reconciliation **all matched, 0 discrepancies** (4 KPI + 14-bucket trend + 6 filter: All→168/26.03/100 %, NAM-SDF→14/42.22, region NAM→42/36.50, Critical→14/89.45, v2025-12-01→12/62.60, missing key→0). Published **only** Shape via `V2/scripts/push-e7d4-shape.ps1` (token DPAPI in-memory) → **v2**, in folder, 10 panels; shell archived at `V2/grafana/dashboards/archive/aegis-forecast-drift-shape-shell.json`. **Oscar visually accepted 2026-07-19**; minor post-acceptance polish (**v3**) on the trend panel only — time start moved to first real data **2024-04-01** (removes empty pre-2024 space) and `lineInterpolation` **smooth → linear**, real per-version points kept, no query/metric/filter/KPI/table/other-dashboard change, KPIs re-verified intact. Overview + Forecast + Performance + other 7 dashboards, datasource, nginx, Docker, CSVs, Python, Power BI, weights, thresholds, alerts, plugins, token, MCP all unchanged; repo secret-free; no manual commit. Token `E7D4_SHAPE_MVP_COMPLETED_VISUALLY_ACCEPTED`. Deliverables `engineering/E7_grafana/E7D4_shape_*` (5 docs). Do **not** start E7D.5. **Open risk R1** unchanged |
| ├ E7D.5 | Stability MVP (analytical panels) | Build | ✅ **Completed — visually accepted** (2026-07-19) — built the Stability dashboard (uid `aegis-forecast-drift-stability` **retained**) from the E7D.0 shell into a full analytical dashboard for the stability drift family (weight 30%) by **cloning the accepted Shape (E7D.4) structure** and swapping the metric to `stability_drift_score`: **8 components / 10 panels** — Average Stability Drift Score (**38.93**, bands 20/40/70), Stability Signals Computable (**168**), Stability Coverage (**100.0 %** = 168/168, green ≥ 0.9), Maximum Stability Drift Score (**100.00**), Stability Drift Score over time (14 forecast-version points, UTC — **ignores Forecast Version**; peak 74.14 @ 2025-12-01, secondary 56.47 @ 2026-04-06 / 55.36 @ 2026-03-07, low 16.08 @ 2026-04-16), Stability Signal Details (per-signal, score desc, full width), Latest Governed Run (1 · Success · 2026-07-13 22:44 UTC · 168 · 71), Data Quality 18/18. **No value hardcoded.** **Data-driven scope:** Stability is 168/168 computable (0 non-computable) → Non-computable Summary panel **intentionally omitted** (Details widened to full width); the snapshot exposes a single `stability_drift_score` with no auxiliary/MAPE metric → 4th KPI is **Maximum Stability Drift Score** and no Current/Previous columns; richer stability auxiliaries (`structural_break_flag`, `cumulative_revision_pct`, `version_count`) populated but intentionally **not** integrated in this single-source MVP (future enhancement). **Reused the E7D.1–E7D.4 mechanism verbatim** (`queryType`-wrapped variables, `v`-prefixed `fv_label`, quoted `run_id`, no aliasing of filtered columns, transform aggregation; boolean fix `convertFieldType is_comp → number` before `mean` on Coverage); accepted Shape v3 polish (time start **2024-04-01**, trend `lineInterpolation` **linear**, points visible) applied from the start. Structural validator **0 real failures**; reconciliation **all matched, 0 discrepancies** (4 KPI + 14-bucket trend + 6 filter: All→168/38.93/100 %, NAM-SDF→14/54.78, region NAM→42/50.71, Critical→14/95.35, v2025-12-01→12/74.14, missing key→0). Published **only** Stability via `V2/scripts/push-e7d5-stability.ps1` (token DPAPI in-memory) → **v2**, in folder, 10 panels; shell archived at `V2/grafana/dashboards/archive/aegis-forecast-drift-stability-shell.json`. Overview + Forecast + Performance + Shape + other 6 dashboards, datasource, nginx, Docker, CSVs, Python, Power BI, weights, thresholds, alerts, plugins, token, DPAPI, MCP all unchanged; repo secret-free; no manual commit. **Oscar visually accepted 2026-07-19** (metrics, trend, details, governance). Token `E7D5_STABILITY_MVP_COMPLETED_VISUALLY_ACCEPTED`. Deliverables `engineering/E7_grafana/E7D5_stability_*` (5 docs) + rebuilt JSON + shell archive + push script. URL `http://localhost:3000/d/aegis-forecast-drift-stability`. **Open risk R1** unchanged |
| ├ E7D.6 | Volatility MVP (analytical panels) | Build | ✅ **Completed — visually accepted (Oscar, 2026-07-19)** — built the Volatility dashboard (uid `aegis-forecast-drift-volatility` **retained**) from the E7D.0 shell into a full analytical dashboard for the volatility drift family (weight 10%), **deliberately NOT a mechanical clone** of Stability/Shape because Volatility is only **partially computable (144/168, coverage 85.7 %)**: **13 panels** — Average Volatility Drift Score (**56.04**, bands 20/40/70), Volatility Signals Computable (**144**), Volatility Coverage (**85.7 %** = 144/168), Maximum Volatility Drift Score (**100.00**), Volatility Drift Score over time (**12** forecast-version points, UTC — **ignores Forecast Version**, axis starts **2024-06-01**; peak 78.13 @ 2026-01-01), Volatility Drift Score by Forecast Key (**horizontal barchart**, NAM-SDF 82.21 top), Volatility Signal Details (per-signal, score desc, full width, shows non-computable rows), Volatility Profile — Governed Auxiliary Metrics (from `forecast_drift_family_scores.csv`: `rolling_stddev/cov/mad`, `oscillation_count`, `sign_change_freq`, `volatility_class`; 144 rows), Non-computable Summary (**INSUFFICIENT_VERSIONS = 24**), Latest Governed Run (1 · Success · 2026-07-13 22:44 UTC · 168 · 71), Data Quality 18/18. **No value hardcoded.** **Data-driven scope:** the 24 non-computable signals are the first two versions of each of the 12 keys (`2024-04-01`, `2024-05-01`), governed by `not_computable_reason = INSUFFICIENT_VERSIONS` → **Non-computable Summary panel IS built** (unlike Stability/Shape at 168/168); **Coverage is a first-class KPI** (denominator = full filtered universe, dynamic); averages/max **exclude** non-computable (never zeroed); governed **auxiliaries live in family_scores.csv (not signals.csv)** → dedicated **Volatility Profile** table honoring only `forecast_key` + `forecast_version` (family_scores has no region/status/run — documented two-table exception); ranking **barchart** is a new panel type; trend starts at first computable bucket **2024-06-01**. **Reused the E7D.1–E7D.5 mechanism verbatim** (`queryType`-wrapped variables, `v`-prefixed `fv_label`, quoted `run_id`, `convertFieldType` before numeric reducers; Infinity requires **`==`** for equality in family_scores filters). Structural validator **0 failures**; reconciliation **all matched, 0 discrepancies** (4 KPI + 12-bucket trend + key ranking + aux 144 + non-computable 24 + 8 filter scenarios incl. non-computable version v2024-04-01 → 12/0/**0 % coverage**). Published **only** Volatility via `V2/scripts/push-e7d6-volatility.ps1` (token DPAPI in-memory) → **v3** (post-acceptance polish), in folder, 13 panels; shell archived at `V2/grafana/dashboards/archive/aegis-forecast-drift-volatility-shell.json`. **Post-acceptance polish (v3):** confirmed trend saved range starts at first real bucket `2024-06-01` (`linear` + `showPoints=always` kept, query/12 buckets unchanged; the "2021" gap was a client-side time override, not the model) and set Volatility Profile `rolling_stddev` (raw 221.6→1,270,234, no governed unit) to explicit **`locale`** unit (thousands-grouped integer) instead of ambiguous `short` K/Mil — no data recalculated, KPIs re-verified. Overview + Forecast + Performance + Shape + Stability + other 5 dashboards, datasource, nginx, Docker, CSVs, Python, Power BI, weights, thresholds, alerts, plugins, token, DPAPI, MCP all unchanged; repo secret-free; no manual commit. Agent browser unauthenticated → live render **visually accepted by Oscar (2026-07-19)**. Token `E7D6_VOLATILITY_MVP_COMPLETED_VISUALLY_ACCEPTED`. Deliverables `engineering/E7_grafana/E7D6_volatility_*` (5 docs) + rebuilt JSON + shell archive + push script. URL `http://localhost:3000/d/aegis-forecast-drift-volatility`. **Stop before E7D.7 (Events).** **Open risk R1** unchanged |
| ├ E7D.7 | Events MVP (governed event log) | Build | ✅ **Completed — visually accepted (Oscar, 2026-07-20)** — transformed the Events dashboard (uid `aegis-forecast-drift-events` **retained**) from the E7D.0 shell into a **read-only** operational view of the governed drift-**event** log. **Data finding:** `forecast_drift_event_history.csv` is a thin 7-col lifecycle table (all `new_status=Open`, single `changed_at`) — **not** the rich source; the **71 events are the `forecast_drift_signals.csv` subset where `is_event=1`**, carrying all rich fields. **13 panels:** Total Events (**71**), Critical (**14**), Warning (**34**), Affected Forecast Keys (**12**), Latest Event, Events by Drift Family donut (shape 26/stability 24/volatility 15/performance 6, non-severity palette), Events by Drift Status donut (Critical 14/Warning 34/Watch 13/Healthy 10, severity palette), Events by Forecast Key barchart (single detection instant → **no over-time trend**), Governed Event Log (71 rows, newest-first, native column-filter search, wrapped Explanation), Latest Governed Run (1·Success·168·71), Data Quality 18/18. **Read-only** — no resolve/ack/close/edit, no invented lifecycle states (only governed `event_status=Open` shown), no invented Severity (`drift_status` = governed band; `severity` alias not surfaced), no invented Service/Scenario/Event Type (empty/single/absent). **6 filters:** forecast_key, forecast_version (via `fv_label`), region, drift_status, run_id, **drift_family** (local) — *documented deviation vs E7D.0 matrix* (Events adds forecast_version + drift_family since events are the signals subset). **Nothing hardcoded** (71 = `is_event=1`; mockup "170" illustrative; CSV is sole truth). Infinity rule reaffirmed: every `filterExpression` column must be selected/computed (`fv_label` needs `forecast_version` selected). Reconciliation **0 discrepancies** (KPIs + distributions + affected keys + explanation 71/71 + event_status Open + max 84.72 + run footer + **9 filter scenarios**). Published **only** Events via `V2/scripts/push-e7d7-events.ps1` (token DPAPI in-memory) → **v4**, in folder, 13 panels, `keepTime=false`/`includeVars=true`, saved range 2026-07-01→07-31 (UTC, picker hidden); shell archived. **Allowed exception:** shared `links` block keeps keepTime=false/includeVars=true. **Visual repair (v5):** Events-by-Drift-Status donut now uses explicit `byName` fixed-color overrides (Healthy green/Watch yellow/Warning orange/Critical red) instead of palette-classic, and Latest Event height reduced h:4→h:3 then restored to h:4 (h:3 clipped the row) (KPIs re-verified unchanged: 71/14/34/12, DQ 18/18). Overview/Forecast/Performance/Shape/Stability/Volatility, datasource, nginx, Docker, CSVs, Python, Power BI, weights, thresholds, alerts, plugins, token, DPAPI, MCP all unchanged; JSON secret-free; no manual commit. **Oscar visually accepted on 2026-07-20** (Latest Event full row, status colors, data + all panels). Deliverables `engineering/E7_grafana/E7D7_events_*` (6 docs) + rebuilt JSON + shell archive + push script. Token `E7D7_EVENTS_MVP_COMPLETED_VISUALLY_ACCEPTED`. URL `http://localhost:3000/d/aegis-forecast-drift-events`. **Stop before E7D.8 (Historical Timeline) — awaiting authorization.** **Open risk R1** unchanged |
| ├ E7D.8 | Historical Timeline MVP (governed chronological view) | Build | ✅ **Completed — visually accepted (Oscar, 2026-07-20)** — rebuilt the Historical Timeline (uid `aegis-forecast-drift-timeline` **retained**) from the E7D.0 shell into a read-only governed timeline. **NO FABRICATED HISTORY:** each of **71** records is a governed drift event (`is_event=1`, run 1) placed by **`forecast_version`** (only field with real temporal spread; 14 versions in signals, **12 among events**, 2024-04→2026-05); single detection instant surfaced as a fact; lifecycle **71 initial Open, 0 transitions** verbatim; no invented Forecast Generated / Alert Triggered activity. **Date Range FUNCTIONAL (key acceptance), proven live:** All 71 / YTD 40 / Last180d 32 / Last90d 5 / Last60d–30d 0 / 2024 5 / 2025 26 / 2026 40 — mechanism (probes 1–7): Infinity ignores native time range and can't cast numbers/dates, so uses `fv_label='v'+forecast_version` + a **custom `date_range` var** filtering by `contains('${date_range:raw}','|'+fv_label+'|')`; native picker hidden. **14 panels** (nav, header, temporal note, 5 KPIs, timeline table, 2 barcharts, details table, latest run, data quality). **7 filters:** forecast_key, forecast_version, region, drift_status, run_id, drift_family, date_range. **GATES A–F pass; reconciliation 0 discrepancies.** Published via `V2/scripts/push-e7d8-timeline.ps1` → v3, `success`, in folder, 14 panels, keepTime=false/includeVars=true/UTC/timepicker hidden; shell archived. Top Forecast Keys / Top Scenarios / Settings & Data Quality NOT started. No CSV/Python/PBI/datasource/nginx/Docker/token/DPAPI/MCP/weights/thresholds change; no manual commit. Before acceptance, Data Quality was rebuilt as the known-good Shape/Stability/Volatility **stat** (`dq = checks_passed + ' / ' + checks_total`, `colorMode background`, no organize transform) after the initial `dq_label`+`lastNotNull` stat rendered “No data”, and Lifecycle Records `textMode`→`value` (republished **v5**). **Visually accepted by Oscar 2026-07-20** (18 / 18 large green, KPIs 71/71/12/12/71, Latest Run, Date Range OK). Token `E7D8_HISTORICAL_TIMELINE_MVP_COMPLETED_VISUALLY_ACCEPTED`. Deliverables `engineering/E7_grafana/E7D8_timeline_*` (7 docs). URL `http://localhost:3000/d/aegis-forecast-drift-timeline` |
| ├ E7D.9A | Top Risk Navigation Consolidation (nav + shell only) | Build | ✅ **Completed — visually accepted (Oscar, 2026-07-20)** — consolidated **Top Forecast Keys** + **Top Scenarios** into a single **Top Risk** section. Canonical = the Top Forecast Keys shell with **UID `aegis-forecast-drift-top-keys` preserved**, renamed *AEGIS Forecast Drift — Top Risk*, retagged `top-risk`/`e7d9a`; shell shows shared nav + 5 filters + header + provisional note + **5 collapsed rows** (Top Forecast Keys / Top Regions / Top Forecast Versions / Top Drift Families / Risk Details, no queries). **Top Scenarios** (uid `aegis-forecast-drift-top-scenarios`) **RETIRED FROM NAVIGATION / ABSORBED INTO TOP RISK** — `aegis-nav` tag removed, queries/UID intact, preserved for rollback with a visible banner. Nav applied to all 11 dashboards; `/api/search?tag=aegis-nav` returns **10** (Top Risk present, Top Scenarios absent). Published via `V2/scripts/push-e7d9a-top-risk-navigation.ps1` → 11/11 `success`; backups under `archive/top-risk-navigation-pre-e7d9a/`. No queries/KPIs built, no deletes, no data/infra/token change, no manual commit. Token `E7D9A_TOP_RISK_NAVIGATION_COMPLETED_VISUALLY_ACCEPTED`. **Stop before E7D.9B.** URL `http://localhost:3000/d/aegis-forecast-drift-top-keys` |
| ├ E7D.9B | Top Risk Dashboard Content (governed rankings & concentration) | Build | ◑ **Implemented — pending Oscar visual acceptance** — converted the Top Risk shell (UID `aegis-forecast-drift-top-keys` preserved) into a governed executive view: 15 analytical panels on `forecast_drift_score` (avg per dimension) + `forecast_drift_family_scores` (standalone). KPIs (Keys 12 · Avg 28.83 · Critical 14 · Events 71), Highest-Risk **NAM-SDF 42.74**, Top Keys/Regions/Versions bars, Top Drift Families + Computability, **Risk Matrix** (key×family, signals native cols — no join), **Consolidated Risk Details** (168 rows + drill links Forecast/Events/Timeline), Latest Run + DQ 18/18. **Cardinality Gate A PASS** (signals 168; family_scores 672=168×4; no join). All rankings/KPIs/matrix reconcile to CSV (zero diffs); ternary computed columns validated live via `/api/ds/query`. Published `V2/scripts/push-e7d9b-top-risk.ps1` → `success` v4; shell archived. Deviations documented (per-key dominant family omitted; family panels honor key+version only; family bar severity-colored). No CSV/infra/weight/threshold change, no other dashboard touched, no manual commit. Token `E7D9B_TOP_RISK_DASHBOARD_IMPLEMENTED_PENDING_VISUAL_ACCEPTANCE`. **Stop before E7D.11.** See `engineering/E7_grafana/E7D9B_top_risk_*` (8 docs). URL `http://localhost:3000/d/aegis-forecast-drift-top-keys` |
| ├ E7D.10 | ~~Top Scenarios~~ — **ABSORBED INTO TOP RISK** (no standalone implementation) | Build | ➖ Retired |
| ├ E7D.11 | Settings & Data Quality panels | Build | ⏳ |
| └ E7D.12 | Integration & final validation | Build | ⏳ |
| **E8** | Production Deployment & Governance (refresh, automation, docs) | Ops | ⏳ |

**Principles:** model-first not tool-first; Information Model before math; Data Dictionary is a deliverable **of** E1 (not a separate stage); reuse Code Improvement (reference, don't copy logic); no live SQL until E1B; this project stays an **independent workspace/repo** from Code Improvement.

**Reuse rule:** Code Improvement = forecasting pipeline engineering. Forecast Drift = drift detection, scoring, alerts, visualization. When Drift needs something from the other project, it **references/reuses**, it does not copy the logic.
